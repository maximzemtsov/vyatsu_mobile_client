import 'package:flutter/material.dart';


class BuildingMapPage extends StatelessWidget {
  const BuildingMapPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text(""),
    );
  }
}